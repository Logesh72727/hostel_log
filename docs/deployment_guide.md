# Deployment Guide

1. Ensure Flutter SDK is installed.
2. Clone repository.
3. Run `flutter pub get`.
4. Configure Firebase as per firebase_setup.md.
5. Run `flutter run` to test on emulator/device.
6. Build release APK with `flutter build apk`.